<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <!-- dev -->
  <springProfile name="dev">
    <include resource="logback-dev.xml"/>
  </springProfile>

  <!-- stag -->
  <springProfile name="stag">
    <include resource="logback-stag.xml"/>
  </springProfile>
</configuration>